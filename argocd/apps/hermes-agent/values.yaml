config:
  model:
    provider: nvidia
    default: z-ai/glm-5.3-flash
  terminal:
    backend: local
  agent:
    gateway_timeout: 1800

env: {}
extraEnvFrom:
  - secretRef:
      name: hermes-agent-provider

persistence:
  enabled: true
  storageClass: longhorn
  size: 10Gi

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    memory: 2Gi
