apiVersion: v1
kind: Service
metadata:
  name: {{ include "garlicdoor.fullname" . }}
  labels:
    {{- include "garlicdoor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "garlicdoor.selectorLabels" . | nindent 4 }}
